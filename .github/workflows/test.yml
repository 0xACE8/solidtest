#
# Copyright (c) 2019-2026 0xACE8
#
# 本ソフトウェアはフリーソフトウェアであり、MITライセンスの下で公開されています。
# 詳細については /LICENSE を参照してください。
#
# https://github.com/0xACE8
#
# 説明: GitHub Actionsのモジュール化を活用し、プライバシーとセキュリティを確保したOpenWrtビルド。
#

name: Build virtual_machines_x64 imwrt 25.12-snapshot kernel 6.12 actions.

on:
  workflow_dispatch:
    inputs:
      restore_config:
        description: 'RESTORE_BAKCONF. 🇯🇵 環境設定復元'
        type: boolean
        default: false
      #boost_wifi_5g:
      #  description: 'BOOST_WIFI5G_SG. 🇯🇵 ５Ｇ信号増幅'
      #  type: boolean
      #  default: false
      upload_firmware:
        description: 'UPLOAD_ARTIFACT. 🇯🇵 成果物の保存'
        type: boolean
        default: false
      upload_release:
        description: 'UPLOAD_RELEASES. 🇯🇵 リリース公開'
        type: boolean
        default: false
      #upload_wenshushu:
      #  description: 'UPLOAD_TO_WENSS. 🇯🇵 文叔叔へ転送'
      #  type: boolean
      #  default: false
      telegram_notify:
        description: 'TELEGRAM_NOTIFY. 🇯🇵 ＴＧ通知送信'
        type: boolean
        default: true
  schedule:
    - cron: '0 2 * * 1'
    - cron: '0 0 1 * *'
    
env:
  ## github token
  GITHUB_TOKEN:         ${{ secrets.ACCESS_TOKEN }}
  ACCESS_TOKEN:         ${{ secrets.ACCESS_TOKEN }}
  RUN_WORKFLOW:         ${{ github.workflow }}
  RUN_REF_NAME:         ${{ github.ref_name }}
  RUN_REF_REPO:         ${{ github.repository }}
  RUNID_NUMBER:         ${{ github.run_id }}
  ## solidwrt core
  GIT_REPO_URL:         ${{ secrets.IMMORTALWRT_SOURCE_LINK }}
  PRIVATE_DATA:         ${{ secrets.SOLIDWRT_CORE }}
  ## immortalwrt 25.12
  GIT_2512_REF:         ${{ secrets.IMMORTALWRT_2512SS_HEAD }}
  PLUGINS_2512:         ${{ github.workspace }}/${{ secrets.PLUGINS_2512_ACE }}
  PATCH_IM2512:         ${{ github.workspace }}/${{ secrets.SRC_PATCH_IM2512 }}
  FEEDS_IM2512:         ${{ github.workspace }}/${{ secrets.ADD_FEEDS_IM2512 }}
  ## immortalwrt 24.10
  GIT_2410_REF:         ${{ secrets.IMMORTALWRT_2410SS_HEAD }}
  PLUGINS_2410:         ${{ github.workspace }}/${{ secrets.PLUGINS_2410_ACE }}
  PATCH_IM2410:         ${{ github.workspace }}/${{ secrets.SRC_PATCH_IM2410 }}
  FEEDS_IM2410:         ${{ github.workspace }}/${{ secrets.ADD_FEEDS_IM2410 }}
  ## github ci scripts
  CHECK_SERVER:         ${{ github.workspace }}/${{ secrets.TEST_SERVER_PERF }}
  INIT_ENV_VAR:         ${{ github.workspace }}/${{ secrets.INIT_ENVIRONMENT }}
  EXPAND_SPACE:         ${{ github.workspace }}/${{ secrets.FREE_DISKS_SPACE }}
  FIRMWARE_ORG:         ${{ github.workspace }}/${{ secrets.ORGANIZE_FW_FILE }}
  CALC_RUNTIME:         ${{ github.workspace }}/${{ secrets.CALC_RUNING_TIME }}
  ## device configure
  DEVICE_PATCH:         ${{ github.workspace }}/${{ secrets.DEVICE_DIFFPATCH }}
  BUILD_CONFIG:         ${{ github.workspace }}/${{ secrets.BUILD_DIFFCONFIG }}
  BACKUP_FILES:         ${{ github.workspace }}/${{ secrets.BACKUP_CONFIGURE }}
  ARGON_COLORS:         ${{ github.workspace }}/${{ secrets.ARGON_THEMECOLOR }}
  ## upload firmware
  UPTO_RELEASE:         ${{ github.workspace }}/${{ secrets.UPLOADTO_RELEASE }}
  UP_WENSHUSHU:         ${{ github.workspace }}/${{ secrets.UPLOAD_WENSHUSHU }}
  ## cache repo
  CACHE_REPOSI:         ${{ github.workspace }}/${{ secrets.CACHE_REPOSITORY }}
  UPLOAD_CACHE:         ${{ github.workspace }}/${{ secrets.UP_COMPILE_CACHE }}
  DWLOAD_CACHE:         ${{ github.workspace }}/${{ secrets.DW_STORAGE_CACHE }}
  ## telegram bot
  TGRAM_NOTIFY:         ${{ github.workspace }}/${{ secrets.PUSH_TO_TELEGRAM }}
  TGRAM_CHATID:         ${{ secrets.TELEGRAM_CHAT_ID }}
  TGRAM_DONEID:         ${{ secrets.TELEGRAM_DONE_ID }}
  ## input status
  INPUT_CONFIG:         ${{ github.event.inputs.restore_config }}
  INPUT_WIFI5G:         ${{ github.event.inputs.boost_wifi_5g }}
  INPUT_UP_ART:         ${{ github.event.inputs.upload_firmware }}
  INPUT_UP_REL:         ${{ github.event.inputs.upload_release }}
  INPUT_UP_WSS:         ${{ github.event.inputs.upload_wenshushu }}
  INPUT_NOTIFY:         ${{ github.event.inputs.telegram_notify }}
  INPUT_KALIVE:         ${{ github.event_name == 'schedule' || github.event.inputs.keepalive_cache == 'true' }}
  COMP_OUTCOME:         ${{ steps.compile.outcome }}
  ## hardware & repo info
  TARGET_MODEL:         virtual_machines_x64
  TARGET_CHIPS:         x86_64
  TARGET_MEDIA:         8g_ssd
  LINUX_KERNEL:         kernel_6.12
  OUTPUT_FILES:         solidwrt-25.12ss-virtual_machines_x64-8gb_ssd
  TZ: "Asia/Tokyo"

jobs:
  build:
    runs-on: ubuntu-24.04

    permissions: write-all

    steps:
    - name: Start Timer (計測開始)
      run: |
        echo "START_TIME=$(date +%s)" >> $GITHUB_ENV

    - name: Checkout Repo A (チェックアウト)
      uses: actions/checkout@main

    - name: Checkout Repo B (Data)
      uses: actions/checkout@main
      with:
        repository: ${{ env.PRIVATE_DATA }}
        token: ${{ env.GITHUB_TOKEN }}
        path: 'private_data'

    - name: Delete Old Workflows (古いワークフローの削除)
      uses: Mattraks/delete-workflow-runs@v2.1.0
      with:
        token: ${{ env.ACCESS_TOKEN }}
        repository: ${{ env.RUN_REF_REPO }}
        retain_days: 0
        keep_minimum_runs: 0
        delete_workflow_pattern: "imwrt 25.12-snapshot kernel 6.12"

    - name: Check Server Performance (サーバー性能の確認)
      id: check_env
      run: |
        find ${{ github.workspace }}/private_data -name "*.sh" -exec chmod +x {} +
        ${{ env.CHECK_SERVER }}

    - name: Sync and Wait for Plugin Repo (プラグインリポジトリの同期と待機)
      run: |
        ${{ env.PLUGINS_2512 }}

    - name: Initialization Environment  (環境の初期化)
      id: init
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        ${{ env.INIT_ENV_VAR }} >/dev/null 2>&1

    - name: Free Disk Space (ディスク空き容量の確保)
      run: |
        ${{ env.EXPAND_SPACE }} >/dev/null 2>&1
        df -Th

    - name: Clone Source Code (ソースコードのクローン)
      working-directory: ./
      run: |
        df -hT /build
        git clone --depth 1 --single-branch $GIT_REPO_URL -b $GIT_2512_REF openwrt
        # Remove China Mirrors (中国国内ミラーの削除)
        cd openwrt
        [ -f "scripts/projectsmirrors.json" ] && sed -i '/.cn\//d; /tencent/d; /aliyun/d' scripts/projectsmirrors.json
        [ -d "files" ] && rm -rf files
        ${{ env.FEEDS_IM2512 }}

    - name: Restore ccache from Private Repo
      env:
        GH_TOKEN: ${{ env.ACCESS_TOKEN }}
        CACHE_TAG_ID: "cache-${{ env.OUTPUT_FILES }}"
      run: |
        ${{ env.DWLOAD_CACHE }} >/dev/null 2>&1

    - name: Update & Install Feeds (フィードの更新とインストール)
      working-directory: ./openwrt
      run: |
        # rtl88x2bu support
        git clone -b openwrt-25.12.0-rc4 https://github.com/mirobiala/rtl88x2bu-cl package/kernel/rtl88x2bu-cl
        ./scripts/feeds update -a
        rm -rf feeds/packages/lang/golang && git clone https://github.com/sbwml/packages_lang_golang -b 26.x feeds/packages/lang/golang
        ./scripts/feeds install -a
        ${{ env.DEVICE_PATCH }}

    - name: Load Custom Configuration (カスタム設定の読み込み)
      working-directory: ./openwrt
      id: loadconfig
      run: |
        cp -rf ${{ env.BUILD_CONFIG }} .config
        ${{ env.PATCH_IM2512 }}
        ${{ env.ARGON_COLORS }}

    - name: Restore Config Archive (設定アーカイブの復元)
      if: steps.loadconfig.outcome == 'success' && env.INPUT_RESTORE == 'true'
      working-directory: ./openwrt
      run: |
        mv ${{ env.BACKUP_FILES }} files
        echo "OUTPUT_FILES=$OUTPUT_FILES-configured" >> $GITHUB_ENV

    - name: Download Packages (パッケージのダウンロード)
      if: steps.loadconfig.outcome == 'success' && !cancelled()
      working-directory: ./openwrt
      id: package
      run: |
        make defconfig
        make download -j$(nproc)
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile Firmware (ファームウェアのコンパイル)
      if: env.INPUT_KALIVE != 'true'
      working-directory: ./openwrt
      id: compile
      env:
        export CARGO_PROFILE_RELEASE_OPT_LEVEL: "3"
        export CARGO_PROFILE_RELEASE_LTO: "true"
        export CARGO_PROFILE_RELEASE_CODEGEN_UNITS: "1"
        # export CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_RUSTFLAGS: "-C target-cpu=cortex-a53 -C target-feature=+crypto,+crc,+neon"
      run: |
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 V=s
        echo "FILE_DATE=$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Organize Files (ファイルの整理)
      if: steps.compile.outcome == 'success'
      id: organize
      run: |
        cd openwrt/bin/targets/*/*
        ${{ env.FIRMWARE_ORG }}
        #echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        #echo "DOWNLOAD=${{ env.OUTPUT_FILES }}.img.gz" >> $GITHUB_ENV
        #echo "TAG_NAME=${{ env.FILE_DATE }}_${{ env.TARGET_CHIP }}_${{ env.DEVICE_NAME }}_${{ env.TARGET_DISK }}" >> $GITHUB_ENV
        #echo "TGRAM_CHATID=${{ env.TGRAM_DONEID }}" >> $GITHUB_ENV

    - name: Upload to Artifacts (Artifactに保存)
      if: steps.organize.outcome == 'success' && env.INPUT_UP_ART == 'true'
      uses: actions/upload-artifact@main
      with:
        name: ${{ env.OUTPUT_FILES }}
        path: ${{ env.FIRMWARE }}

    - name: Upload to Release (Releaseに公開)
      if: steps.compile.outcome == 'success' && env.INPUT_UP_REL == 'true'
      uses: softprops/action-gh-release@master
      with:
        token: ${{ env.ACCESS_TOKEN }}
        tag_name: ${{ env.TAG_NAME }}
        name: ${{ env.TARGET_CHIPS }}_${{ env.TARGET_MODEL }}_${{ env.TARGET_MEDIA }}_${{ env.LINUX_KERNEL }}_${{ env.FILE_DATE }}
        files: ${{ env.FIRMWARE }}/*
        body: |
          ${{ env.UPTO_RELEASE }}

    - name: Upload to WenShuShu (ウェンおじさんにアップロードする)
      if: steps.compile.outcome == 'success' && steps.organize.outcome == 'success' && env.INPUT_UP_WSS == 'true'
      working-directory: ./openwrt
      run: |
        ${{ env.UP_WENSHUSHU }}

    - name: Calculate Duration (実行時間の計算)
      if: always() && env.INPUT_NOTIFY == 'true' && env.INPUT_KALIVE != 'true'
      run: |
        ${{ env.CALC_RUNTIME }}

    - name: Final Report & TG Notify (総合ステータス通知)
      if: always() && env.INPUT_NOTIFY == 'true' && env.INPUT_KALIVE != 'true'
      run: |
        ${{ env.TGRAM_NOTIFY }}

    - name: Archive and Upload CACHE (自動生成された究極のビルドキャッシュ)
      if: success() && !cancelled()
      working-directory: ./openwrt
      env:
        GH_TOKEN: ${{ env.ACCESS_TOKEN }}
      run: |
        ${{ env.UPLOAD_CACHE }}

    - name: Delete Old Releases (古いリリースの削除)
      if: steps.compile.outcome == 'success' && env.INPUT_UP_REL == 'true'
      uses: dev-drprasad/delete-older-releases@master
      with:
        keep_latest: 2
        delete_tags: true
        delete_tag_pattern: "${{ env.TARGET_MODEL }}_${{ env.GIT_HEAD_REF }}" 


